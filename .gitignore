# ambientes virtuais
.venv_xla/
.venv_tvm/

# notas internas de desenvolvimento — não publicar no GitHub
docs/dev-notes/

# saídas do benchmark (JSONs soltos de execuções ad-hoc).
# As campanhas de medição que sustentam o artigo ficam VERSIONADAS em
# results/ — são os dados brutos do artefato (selo de reprodutibilidade).
out_*.json

# python
__pycache__/
*.pyc

# HLO não-otimizado é gigante (~93 MB, pesos embutidos como literais);
# mantenha no repositório apenas os artefatos que forem citados no artigo.
ir_dumps/xla/*/fused_jit_unoptimized.hlo
